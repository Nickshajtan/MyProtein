//Variables
const ajaxurl           = hcc_ajax_params.ajaxurl;
const home_url          = hcc_js_custom_params.home_url;
const is_404_url        = hcc_js_custom_params.is_404;
const error_header      = hcc_ajax_params.error_header;
const error_body        = hcc_ajax_params.error_body;
const success_header    = hcc_ajax_params.success_header;
const success_body      = hcc_ajax_params.success_body;
const cfThankYou        = hcc_ajax_params.cfThankYou;
const more_text         = hcc_ajax_params.more_text;
const error_text        = hcc_ajax_params.error_text;
const load_text         = hcc_ajax_params.load_text;
const customModalStatus = ( hcc_ajax_params.cf_modal_status === 1 ) ? true : false;
const ajaxLoadingStatus = ( hcc_ajax_params.ajax_loading === 1 )    ? true : false;
